# File Nexus Suite — Dependencies
# pip install -r requirements.txt

# ── Required ──────────────────────────────────────────────────────────
PySide6>=6.4.0          # GUI framework (includes QtSvg for automatic SVG icon support)

# ── Optional — Works without these, with limited features ─────────────
chardet>=4.0.0          # Automatic text file encoding detection
python-docx>=1.0.0      # DOCX file reading (Text Merger)
pdfplumber>=0.10.0      # PDF text extraction (Text Merger)
openpyxl>=3.1.0         # XLSX file reading (Text Merger)
python-hwpx>=2.9.0      # HWPX file reading (Text Merger, v1.0.6+, Korean document standard)

# ── Build — Required only for building ────────────────────────────────
pyinstaller>=6.0.0      # Standalone executable build
